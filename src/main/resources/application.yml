server:
  port: 8088

spring:
  datasource:
    url: jdbc:h2:~/Downloads/alunodatabase;DB_CLOSE_ON_EXIT=FALSE
    username: aluno
    password: aluno

api:
  security:
    token:
      secret: ${JWT_SECRET:my-secret-key}

flyway:
  enabled: true
  locations: classpath:db/migration
  baseline-on-migrate: true
  outOfOrder: true
